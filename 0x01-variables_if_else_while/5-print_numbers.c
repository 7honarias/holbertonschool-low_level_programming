/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
#include <stdio.h>
int main(void)
{
  int a;
  for (a = 0; a < 10; a++)
    {
      printf("%i",a);
    }
  putchar('\n');
  return (0);
}
